---
- name: Restart postgres
  shell: su - postgres -c "pg_ctl restart -D {{postgres_data_directory}}"

- name: Restart mysql
  service:
    name: "{{ mysql_service_name }}"
    state: restarted

- name: Restart mariadb
  service:
    name: "{{ mariadb_service_name }}"
    state: restarted
